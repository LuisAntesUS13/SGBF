export enum RUTA_GLOBAL {
  PATH = 'bitacora',
}

export enum JWT {
  SECRET = '21c7e331-949a-5fdc-b364-1299ef81a298',
}

export enum paginador {
  pagina_acual = 1,
  registros_por_pagina = 10,
}

export enum folderArchivos {
  ruta = 'C:/gestionBitacoras/',
}

export enum tipoArchivos {
  pdf = 1,
}

export enum ipDefecto {
  ip = '127.0.0.1',
}
